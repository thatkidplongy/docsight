const BenchmarkPage = () => (
  <section>
    <h1 className="text-3xl font-semibold text-white">Benchmark</h1>
    <p className="mt-3 max-w-2xl text-neutral-400">
      Accuracy, citation validity and refusal rates for each model, measured against a labelled gold set. Lands in phase
      5.
    </p>
  </section>
);

export default BenchmarkPage;
